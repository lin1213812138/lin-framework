# Git 提交信息规范

自动生成的 Git 提交信息必须遵循以下规则：

## 语言

提交信息 **必须使用中文**。

## 信息结构

每次提交**必须包含 header（标题）**，当变更涉及多个维度或有值得说明的上下文时，**必须附加 body（正文）**。

```
<type>: <中文标题>

<中文正文 - 每行一个要点>
```

- header 不超过 100 个字符
- header 和 body 之间**必须空一行**
- body 每行一个要点，使用 `- ` 前缀
- body 描述「为什么改」而非「改了哪些文件」

## type

| type | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 |
| `docs` | 文档 |
| `style` | 样式 |
| `refactor` | 重构 |
| `perf` | 性能优化 |
| `test` | 测试 |
| `build` | 构建 |
| `ci` | CI/CD |
| `chore` | 杂项 |
| `revert` | 回退 |

## 示例

### 单行（变更单一，无需 body）

```
fix: 出库单状态更新时未同步托盘状态
```

### 多行（变更涉及多个方面）

```
fix: 修复系统管理用户列表字段取值从 name 改为 userName

- 用户管理列表头像改用 userName 字段
- 用户组/站点编辑弹窗选项显示为 userName(账号)
- 站点/组织用户列表 label 改用 userName
- tag-custom-box 添加 size="small" 属性
- 新增 .cursorrules 提交规范文件
```

## 约束

- header 不超过 100 个字符
- type 后跟英文冒号和空格
- header 不为空
- 有多方面变更时，body 不可省略